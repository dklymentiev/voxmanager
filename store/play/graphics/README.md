# Play graphics, required sizes

Put final art here (commit deliberately; large binaries are otherwise git-ignored).

| Asset | Size | Format | Notes |
|---|---|---|---|
| App icon | 512×512 | 32-bit PNG | The Aurora-Mic on the dark brand tile. Generate from `../../../brand/icon/aurora-mic.svg`. |
| Feature graphic | 1024×500 | PNG/JPG, no alpha | Banner: gradient mic + "Vox Manager, Voice to PC". Use brand colors. |
| Promo (optional) | 180×120 | PNG/JPG | Legacy, usually skippable. |

Source of truth for colors/icon: [`../../../brand/`](../../../brand/). The 512 icon
can be exported from the SVG master; the feature graphic is a new composition to be
designed (mic + wordmark on the brand gradient).
